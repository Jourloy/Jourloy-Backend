version: '3.7'

services:
    app:
        container_name: Jourloy-Backend
        build:
            context: .
        volumes:
            - .:/app
            - /app/node_modules
        command: yarn dev
        env_file:
            - .env
        ports:
            - "${PORT}:${PORT}"
        restart: always

volumes:
    app:
